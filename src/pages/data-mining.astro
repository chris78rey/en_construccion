---
// --- INICIO CAMBIO NUEVO ---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionIntro from '../components/SectionIntro.astro';
import FaqAccordion from '../components/FaqAccordion.vue';
import { faqData } from '../data/site';
---

<BaseLayout
  title={`Da-TIca | Data Mining`}
  description={`Análisis de datos orientado a patrones, claridad operativa y decisiones más útiles.`}
  pathname={Astro.url.pathname}
>
  <section class={`hero page-hero`}>
    <div class={`container hero-grid`}>
      <div class={`hero-copy`}>
        <span class={`eyebrow`}>Data Mining</span>
        <h1>Datos mejor entendidos para decidir con más seguridad.</h1>
        <p>
          Esta línea ayuda a ordenar, revisar y analizar información para encontrar patrones,
          errores, oportunidades y señales útiles que muchas veces pasan desapercibidas.
        </p>
        <div class={`button-row`}>
          <a class={`button`} href={`mailto:crrb@da-tica.com?subject=Quiero%20informaci%C3%B3n%20sobre%20Data%20Mining`}>Solicitar información</a>
          <a class={`button button-secondary`} href={`/`}>Volver al inicio</a>
        </div>
      </div>

      <div class={`glow-card`}>
        <p class={`mini-label`}>Resultado esperado</p>
        <h2>Menos desorden y más información útil para actuar</h2>
        <div class={`metric-grid single-column`}>
          <article><strong>Orden</strong><span>Información más clara y mejor estructurada</span></article>
          <article><strong>Patrones</strong><span>Hallazgos que no saltan a simple vista</span></article>
          <article><strong>Decisión</strong><span>Base más sólida para priorizar acciones</span></article>
        </div>
      </div>
    </div>
  </section>

  <section class={`section`}>
    <div class={`container`}>
      <SectionIntro
        eyebrow={`Dónde aporta valor`}
        title={`Aplicaciones prácticas del análisis de datos`}
        text={`El enfoque está en resolver problemas reales y convertir información dispersa en señales que ayuden a mejorar procesos y decisiones.`}
      />
      <div class={`card-grid card-grid-3`}>
        <article class={`info-card`}>
          <h3>Detectar inconsistencias</h3>
          <p>Permite encontrar vacíos, duplicados, errores repetitivos o registros fuera de comportamiento esperado.</p>
        </article>
        <article class={`info-card`}>
          <h3>Entender mejor la operación</h3>
          <p>Ayuda a identificar cuellos de botella, concentraciones, tendencias y oportunidades de mejora.</p>
        </article>
        <article class={`info-card`}>
          <h3>Preparar el crecimiento</h3>
          <p>Una mejor lectura de datos facilita luego reportes, automatización e inteligencia artificial aplicada.</p>
        </article>
      </div>
    </div>
  </section>

  <section class={`section section-soft`}>
    <div class={`container two-col-grid`}>
      <article class={`info-card`}>
        <SectionIntro
          eyebrow={`Cómo se trabaja`}
          title={`Una ruta simple para convertir datos en hallazgos útiles`}
          text={`El proceso busca claridad, practicidad y resultados entendibles.`}
        />
        <ul class={`detail-list`}>
          <li>Recepción y revisión de las fuentes de información.</li>
          <li>Limpieza, organización y análisis exploratorio.</li>
          <li>Detección de patrones, inconsistencias y oportunidades.</li>
          <li>Entrega de hallazgos accionables y posibles caminos de mejora.</li>
        </ul>
      </article>

      <article class={`info-card accent-card`}>
        <p class={`mini-label`}>Enfoque</p>
        <h3>Más claridad y menos complejidad innecesaria</h3>
        <p>
          El valor está en traducir datos confusos o dispersos en información entendible,
          útil y cercana a la realidad del negocio o del proceso.
        </p>
      </article>
    </div>
  </section>

  <section class={`section`}>
    <div class={`container`}>
      <SectionIntro eyebrow={`Preguntas frecuentes`} title={`Dudas comunes sobre Data Mining`} text={`Estas respuestas ayudan a entender mejor cómo puede aportar este servicio.`} />
      <FaqAccordion items={faqData} client:visible />
    </div>
  </section>
</BaseLayout>
// --- FIN CAMBIO NUEVO ---